[project]
name = "sample_project_b"
version = "0.1.0"
description = "Another simple project"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "Your Name", email = "you@example.com" }
]

[tool.psydk]
data_dir = "data"

[tool.psydk.tasks]
my_task = { fun = "tmod:main_function" }
