# This project was generated with 0.3.22 using template: https://github.com/beeware/briefcase-template@v0.3.22
[tool.briefcase]
project_name = "Psydk Launcher"
bundle = "com.psydklauncher"
version = "0.0.1"
url = "https://docs.psydk.dev"
license.file = "LICENSE"
author = "Jane Developer"
author_email = "marc@psydk.dev"

[tool.briefcase.app.psydklauncher]
formal_name = "Psydk Launcher"
description = "..."
long_description = """More details about the app should go here.
"""
sources = [
    "src/psydklauncher",
]
test_sources = [
    "tests",
]
requires = ["numpy<2", "pandas<2", "pillow<12"]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.psydklauncher.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.psydklauncher.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.psydklauncher.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.psydklauncher.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.psydklauncher.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.psydklauncher.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.psydklauncher.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.psydklauncher.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.psydklauncher.iOS]
requires = [
     # "--find-links=/Users/marc/repos/psydk/crates/psydk/target/wheels/",
     "psydk",
]
min_os_version = "18.0"

[tool.briefcase.app.psydklauncher.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.psydklauncher.web]
requires = [
    # Add your web-specific app requirements here
]
