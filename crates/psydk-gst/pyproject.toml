[project]
name = "psydk-gst"
version = "1.24.11-2"
description = "GStreamer binaries for different platforms"
license = {"text"= "LGPL-2.0-or-later"}

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"


[tool.setuptools.packages.find]
where = ["src"]
